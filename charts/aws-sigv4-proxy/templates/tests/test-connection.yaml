apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aws-sigv4-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "aws-sigv4-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aws-sigv4-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
